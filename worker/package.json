{
  "name": "pizzaria-premium-mp-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker — backend seguro para Pix via Mercado Pago. Access Token fica em secret, nunca exposto ao client.",
  "private": true,
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.78.0"
  }
}
